export interface Customer {
    id: string,
    firstName?: string,
    lastName?: string,
    birthDate?: number,
    address?: number,
    status?: number,
    username?: string,
    password?: string,
    email?: string,
    phoneNumber?: string,
    
}