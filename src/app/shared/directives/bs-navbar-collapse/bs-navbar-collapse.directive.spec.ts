import { BsNavbarCollapseDirective } from './bs-navbar-collapse.directive';

describe('BsNavbarCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new BsNavbarCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
